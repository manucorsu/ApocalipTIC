using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HPBarScript : MonoBehaviour
{
    //[SerializeField] private float Max { get; private set; }
    //[SerializeField] private float Min { get; private set; }
    //[SerializeField] private float Value { get; private set; }

    //public void Change(float v)
    //{
    //    Value = Mathf.Clamp(Value + v, 0, Max);
    //}
}
